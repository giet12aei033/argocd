apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
  namespace: dev  # optional – use only if targeting a specific namespace
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  MAX_RETRIES: "5"
